/**
 * This is just <%= whatIsThis %>.
 *
 */

var fluxDispatcher = require('flux/lib/Dispatcher'),
  extend = require('react/lib/Object.assign'),
  Dispatcher = new fluxDispatcher();

module.exports = extend(Dispatcher, {
  /* dispatcher layer code here*/
});
